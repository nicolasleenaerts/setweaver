% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jtct.R
\name{jtct}
\alias{jtct}
\title{jtct}
\usage{
jtct(y, x)
}
\arguments{
\item{y}{Outcome vector (binary: 0/1 or logical). Must be the same length as
`x`.}

\item{x}{Predictor vector (binary: 0/1 or logical). Must be the same length
as `y`.}
}
\value{
An integer scalar giving the number of observations where `x == 1`
and `y == 1`, after excluding missing values.
}
\description{
Counts the number of complete observations where both a binary
outcome `y` and a binary predictor `x` equal 1. Missing values are excluded
pairwise (rows with `NA` in either `x` or `y` are ignored).
}
\examples{
cprob_inv(misimdata$y,misimdata$x1)
}
